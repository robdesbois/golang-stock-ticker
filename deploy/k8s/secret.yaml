# Placeholder only — DO NOT put a real Alpha Vantage API key in this file or commit one.
# Populate manually after applying, e.g.:
#   kubectl create secret generic stockticker-secret \
#     --from-literal=APIKEY=your-real-key --dry-run=client -o yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: stockticker-secret
type: Opaque
stringData:
  APIKEY: "REPLACE_WITH_YOUR_ALPHAVANTAGE_API_KEY"
